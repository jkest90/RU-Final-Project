var maps = require('./maps.controller.js');

module.exports = (app) => {

    // app.get('/api/address', maps.getAddress);
    // app.get('/api/directions', maps.getDirections);
    // app.get('/api/places', maps.getPlaces);
    // app.get('/api/matrix', maps.getMatrix)
}
